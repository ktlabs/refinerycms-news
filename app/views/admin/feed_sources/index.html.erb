<div id='actions'>
  <ul>
    <li>
      <%= render :partial => "/shared/admin/search", :locals => {:url => admin_news_items_url} %>
    </li>
    <li>
      <%= link_to t('.create'), new_admin_feed_source_url, :class => "add_icon" %>
    </li>
  </ul>
</div>
<div id='records'>
  <% if searching? %>
    <h2><%= t('shared.admin.search.results_for', :query => params[:search]) %></h2>
  <% end %>
  <% if @feed_sources.any? %>
    <%= will_paginate @feed_sources %>
    <ul>
      <%= render :partial => "feed_source", :collection => @feed_sources %>
    </ul>
    <%= will_paginate @feed_sources %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong><%= t('.no_items_yet') %></strong>
      <% else %>
        <%= t('shared.admin.search.no_results') %>
      <% end %>
    </p>
  <% end %>
</div>
